export default function PlaceholderView({ title }) {
  return (
    <section className="placeholder-view card">
      <h2>{title}</h2>
      <p>This section is not set up yet. Use Dashboard or Add Member for now.</p>
    </section>
  );
}
